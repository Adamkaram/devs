name: CI

on:
  push:
    branches: [ \"main\" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run tests
        run: |
          echo \"Running tests...\"
          # هنا تحط أوامر الاختبار
